"settings"
{
    // Флаг имунитета (не нужно указывать флаг: z)
    "immunity_level"                    ""

    // Новое имя, в случае пустого псевдонима, после обработки
    "empty_name"                        "unnamed"

    // Удаление флуда сообщениями о смене ника
    "remove_flood"                      "1"
}